"npm install" to install all required packages
"npm start" to start the server.

Put other settings.json files inside "face-task1\src\Components\Settings". One is already there.
Call file through url: "http://localhost:3000/face?setting=settings2.json"
Otherwise default settings.json file will be used.

Final object and array are commented in the code.
